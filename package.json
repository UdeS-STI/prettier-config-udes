{
  "name": "prettier-config-udes",
  "version": "0.0.3",
  "description": "Prettier shareable config for the UdeS style guide",
  "keywords": [
    "udes",
    "code",
    "code style",
    "enforce",
    "quality",
    "strict",
    "style",
    "config",
    "prettier"
  ],
  "homepage": "https://github.com/UdeS-STI/prettier-config-udes",
  "bugs": {
    "url": "https://github.com/UdeS-STI/prettier-config-udes/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/UdeS-STI/prettier-config-udes.git"
  },
  "license": "MIT",
  "author": "Université de Sherbrooke",
  "main": "index.js",
  "files": [
    "index.js"
  ],
  "scripts": {
    "format": "npm-run-all format:*",
    "format:eslint": "eslint --ignore-path .gitignore . --ext js,json,md --fix",
    "format:prettier": "prettier --ignore-path .gitignore --config=index.js --write '**/*.{js,json,md}'",
    "lint": "npm-run-all lint:*",
    "lint:eslint": "eslint --ignore-path .gitignore . --ext js,json,md",
    "lint:prettier": "prettier --ignore-path .gitignore --config=index.js --check '**/*.{js,json,md}'"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "post-commit": "git update-index -g"
    }
  },
  "dependencies": {
    "@prettier/plugin-php": "^0.14.0",
    "prettier": "^2.0.5"
  },
  "devDependencies": {
    "eslint-config-udes": "^0.4.0",
    "eslint-plugin-json-format": "^2.0.1",
    "eslint-plugin-markdown": "^1.0.2",
    "eslint-plugin-prettier": "^3.1.3",
    "husky": "^4.2.5",
    "lint-staged": "^10.2.2",
    "npm-run-all": "^4.1.5"
  }
}
